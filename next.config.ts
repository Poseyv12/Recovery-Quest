/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['ljiizjvrdthzppfdmiqs.supabase.co'], // replace with your actual Supabase project domain
  },
}

module.exports = nextConfig